<Page x:Class="TheMovies.Views.MovieList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TheMovies.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MovieList">
    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Heading -->
            <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Foreground="DarkRed" Text="★THE MOVIES★" FontSize="40" FontWeight="Heavy" FontFamily="Verdana" HorizontalAlignment="Center" Margin="20"/>

        <!-- Movie list -->
        <ListBox Grid.Row="1"
             ItemsSource="{Binding Movies}">

            <ListBox.ItemTemplate>
                <DataTemplate>

                    <Border BorderThickness="1" Margin="2">

                        <StackPanel>

                            <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="DemiBold"/>


                            <TextBlock Text="{Binding Duration, StringFormat=Varighed: {0} min}"/>

                            <TextBlock Text="{Binding Genre, StringFormat=Genre: {0}}"/>

                            <TextBlock Text="{Binding Director, StringFormat=Instruktør: {0}}"/>

                            <TextBlock Text="{Binding Premierdate, StringFormat=Premiere: {0:d}}"/>

                        </StackPanel>

                    </Border>

                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

        <!-- Back button -->
        <Button Grid.Row="2" HorizontalAlignment="Left" Content="🡠 Tilbage" Width="120" Command="{Binding GoMovieBackCommand}" Margin="2"/>


    </Grid>
</Page>
